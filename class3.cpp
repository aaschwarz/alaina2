#include <studio.h>

int main() {
    if(x>y) {
        printf("x is greater than y ");
    }
    if(x<y) {
        printf("x is less than y");
    }
    if(x==y) {
        printf("x is equal to y");
    }
    return 0;
}